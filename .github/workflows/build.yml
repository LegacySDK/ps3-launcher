name: Build PS3 Launcher

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: dpenendecies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            git \
            wget \
            curl \
            unzip \
            make \
            gcc \
            g++ \
            python3

      - name: Install PSL1GHT SDK
        shell: bash
        run: |
          curl -fsSL https://miskaa.pl/legacysdk/sh/setup.sh | sudo bash
      
          echo "PS3DEV=/opt/ps3dev" >> "$GITHUB_ENV"
          echo "PSL1GHT=/opt/ps3dev/INSTALL_PSL1GHT" >> "$GITHUB_ENV"
      
          echo "/opt/ps3dev/ppu/bin" >> "$GITHUB_PATH"
          echo "/opt/ps3dev/spu/bin" >> "$GITHUB_PATH"

      - name: build ps3soundlib
        shell: bash
        run: |
          git clone https://github.com/wargio/ps3soundlib
          cd ps3soundlib
          make

      - name: build tiny3D
        shell: bash
        run: |
          git clone https://github.com/wargio/tiny3D.git
          cd tiny3D
          make

      - name: build
        shell: bash
        run: |
          make clean
          make

      - name: upload
        uses: actions/upload-artifact@v4
        with:
          name: ps3-launcher
          path: |
            *.elf
            *.self
            *.fake.self
